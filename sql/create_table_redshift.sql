CREATE TABLE sales
(
    order_id INT,
    order_date DATE,
    customer TEXT,
    product TEXT,
    category TEXT,
    quantity INT,
    unit_price DECIMAL(10,2),
    region TEXT,
    total_sale DECIMAL(10,2)
);